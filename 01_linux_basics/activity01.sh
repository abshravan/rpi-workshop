#!/usr/bin/env bash
# Activity 01 — Your first folder on the Pi
# Run these commands one at a time in your terminal.

mkdir workshop
cd workshop
nano notes.txt
# Type something memorable, then:  Ctrl-O  →  Enter  →  Ctrl-X  to save & exit

cat notes.txt
# Expected output: whatever you typed above
